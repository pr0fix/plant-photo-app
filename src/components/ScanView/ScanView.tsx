import React from "react";
import CameraScreen from "./CameraScreen";

/**
 * ScanView component which serves as the camera screen container.
 * Handles both new plant photos and editing existing plant photos.
 *
 * @returns {JSX.Element} - CameraScreen component with photo capture functionality.
 */
const ScanView = (): JSX.Element => {
  return <CameraScreen />;
};

export default ScanView;
